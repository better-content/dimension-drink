package com.example.examplemod;

// This example config was disabled to prevent loading the example mod alongside Obelisks.
// Keeping the file to preserve project history; it contains no active code.
public final class Config {
    private Config() {}
}
